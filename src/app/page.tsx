/**
 * Placeholder root page so the scaffold builds and runs.
 * The real landing/auth experience is built in a later phase.
 */
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-3 p-8 text-center">
      <h1 className="text-4xl font-semibold tracking-tight">Atlas</h1>
      <p className="text-gray-500">Never forget who you met again.</p>
      <p className="text-sm text-gray-400">
        Phase 1 scaffold — application features coming soon.
      </p>
    </main>
  );
}
