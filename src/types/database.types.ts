/**
 * Generated Supabase database types — PLACEHOLDER.
 *
 * Once the Atlas schema (profiles, people, events, event_people, tags,
 * person_tags, follow_ups) exists in the database, regenerate this file with
 * the Supabase CLI:
 *
 *   supabase gen types typescript --project-id <project-ref> > src/types/database.types.ts
 *
 * Then parameterise the Supabase clients, e.g.:
 *   createBrowserClient<Database>(...)
 *   createServerClient<Database>(...)
 *
 * to get fully typed queries.
 */
export type Database = Record<string, never>;
