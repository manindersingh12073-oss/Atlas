// Tailwind CSS v4 is configured through PostCSS.
// All theme/customization lives in CSS (see src/app/globals.css), not a JS config.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
